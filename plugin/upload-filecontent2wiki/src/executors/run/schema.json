{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Run executor",
  "description": "",
  "type": "object",
  "properties": {
    "accessToken": {"type": "string"},
    "localJsonfilePath": {"type": "string"},
    "gitLabBaseUrl": {"type": "string"},
    "uploadRepoId": {"type": "number"}
  },
  "required": [
    "accessToken",
    "localJsonfilePath",
    "gitLabBaseUrl",
    "uploadRepoId"
  ]
}
